# Load drivetemp at boot so SATA drive temperatures are exposed via hwmon
# (lm_sensors, Beszel agent, Cockpit). Without this, drive temps disappear
# after a reboot. See docs: "hardware health monitoring tools".
drivetemp
